import { Header, Navbar } from "@components";
import { Brands } from '@components/brands';

export const Page = () => {
    return (
        <div>
            <Header />
            <Brands />
            <Navbar />
        </div>
    )
}